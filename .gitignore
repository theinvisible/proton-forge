# Build directories
build/
cmake-build-*/
debian-build/

# GitHub Actions - keep workflows
!.github/
!.github/workflows/

# IDE files
.idea/
.vscode/
*.user
*.pro.user
*.swp
*.swo
*~
.DS_Store

# Qt generated files
*_autogen/
moc_*.cpp
ui_*.h
qrc_*.cpp
*.autosave
*.qm

# Compiled files
*.o
*.so
*.a
*.exe
ProtonForge
NvidiaAppLinux

# CMake
CMakeCache.txt
CMakeFiles/
cmake_install.cmake
Makefile
*.cmake
!CMakeLists.txt

# Cache
.cache/

# Packages
*.deb
*.rpm
*.tar.gz
*.tar.xz
*.zip
# …except the ZIP fixtures: they are test data the unit tests open by path, and
# the blanket *.zip above is what kept them out of the repo and CI red while
# they sat in the working tree looking committed.
!tests/steam-lab/fixtures/gog/*.zip

# Generated files
generated/
Version.h

# Logs and temporary files
*.log
*.tmp
*.bak
*.backup

# Test lab
#
# The lab's own configuration — it may hold a GitHub token. Its work directory is
# outside the repository by design (see PF_LAB_DIR in TESTS.md), so there is
# nothing else here to ignore.
tests/steam-lab/lab.env

# Packaging staging directory
debian-build/
